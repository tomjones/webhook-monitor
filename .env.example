# Database connection string (required)
# Heroku sets this automatically when you add the PostgreSQL addon
DATABASE_URL=postgres://user:password@localhost:5432/webhook_monitor

# Server port (optional, defaults to 3000)
PORT=3000

# Webhook retention in days (optional, defaults to 90)
RETENTION_DAYS=90
